import app from "./src/app.js";

// The app is already configured to listen on the specified port
// This file just imports and exports the app for potential testing or other uses
export default app;
